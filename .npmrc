# Hoist next and related packages to root node_modules so eslint-config-next
# can resolve them in a pnpm monorepo (eslint-config-next uses CJS require for next internals)
hoist-pattern[]=next
public-hoist-pattern[]=next
