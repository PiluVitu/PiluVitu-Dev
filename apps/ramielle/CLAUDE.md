# CLAUDE.md — `apps/ramielle`

Guidance para o Worker **ramielle** (`@piluvitu/ramielle`). O Claude Code carrega este arquivo **junto** com o `CLAUDE.md` da raiz — aqui ficam só os detalhes deste workspace; orquestração/monorepo/CI/dependency policy estão na raiz. Cada fato mora num único arquivo: onde um assunto já está documentado em `apps/financas/CLAUDE.md` ou `apps/api/CLAUDE.md`, este arquivo aponta pra lá em vez de repetir.

## O que é, e a regra de corte contra o promeia

`apps/ramielle` é um **Cloudflare Worker** (Hono + D1 SQLite) que está substituindo, fatia a fatia, a API Go (`apps/api`) — hoje: votação de filmes (auth Google, sessões de votação), auth do admin. Roda na borda da Cloudflare, sem disco persistente próprio; todo estado vive no D1.

**Regra de corte contra `apps/promeia`** (serviço Python que roda no Mac do dono): a pergunta decisiva é **"isto precisa de GPU, de um modelo de linguagem local, ou de um arquivo em disco?"**

- **Sim** ⇒ mora no `apps/promeia` (ex.: o insight financeiro via Ollama — precisa de GPU/Metal, que nenhum Worker/Container Cloudflare oferece; ver `apps/financas/CLAUDE.md` § _Insight de IA_ e `apps/promeia/CLAUDE.md`).
- **Não** ⇒ mora no ramielle (login, sessão, CRUD de votação — tudo isso é HTTP + SQL, sem nenhuma das três dependências acima).

## Estado da fatia ① (auth) — COMPLETA

Construído em tasks sequenciais, plano em `.superpowers/sdd/2026-07-28-ramielle-fatia1-auth/` (relatórios de task são gitignored — os achados que precisavam sobreviver foram trazidos pra este arquivo). ⚠️ Cabeçalho e T5 corrigidos na revisão final da fatia ② (M4) — ficaram congelados como se esta ainda fosse "a fatia atual" (T5 marcada "(esta)") depois que a ② (abaixo) já tinha sido completada e revisada.

- **T1** — esqueleto do Worker (Hono + D1 + envelope `{ok,data,notifications}` + `/health`), D1 `piluvitu-ramielle` criado de verdade (`wrangler d1 create`), job `ramielle` no CI (`.github/workflows/ci.yml`).
- **T2** — `migrations/0001_votacao.sql`: schema da votação portado do Go (6 tabelas, 5 índices).
- **T3** — Better Auth com Google (`migrations/0002_better_auth.sql`, `src/lib/auth.ts`) — votação **LIVRE**, sem allowlist de acesso.
- **T4** — `GET /auth/me`, `POST /auth/logout`, guards `requireAuth`/`requireAdmin` (`src/lib/session.ts`), ponte `user`↔`users` (`src/domain/users.ts`).
- **T5** — CORS com credenciais + a documentação desta fatia.

**Nada em produção mudou nesta fatia.** `apps/web` continua falando com a API Go em `promeia.piluvitu.com.br` (ver `apps/api/CLAUDE.md`). O ramielle sobe, responde `/health` contra o D1, autentica com Google aceitando qualquer conta, distingue admin por `ADMIN_EMAILS` a cada request, e responde CORS com credenciais para `piluvitu.com.br` — tudo isso hoje só é alcançável localmente, até o dono completar as pendências no fim deste arquivo.

## Estado da fatia ② (votação) — COMPLETA

Plano em `.superpowers/sdd/2026-07-29-ramielle-fatia2-votacao/` (relatórios de task são gitignored — os achados que precisavam sobreviver estão aqui). Paridade contra o Go é o critério de aceitação de TODA rota desta fatia — `apps/web` em produção ainda fala com a API Go, nada muda até o cutover (fatia ④).

- **T1** — camada de fio (`src/lib/wire.ts`): `sessionToWire`/`movieToWire` (D1 snake_case → PascalCase, vetor dourado provado contra fixture do Go).
- **T2** — `GET /votacao/sessions` e `GET /votacao/sessions/{id}` (`src/routes/votacao.ts`, `src/domain/sessions.ts`).
- **T3** — `POST /votacao/sessions/{id}/votes` (`src/domain/votes.ts#replaceUserVotes`, rota em `src/routes/votacao.ts`) — voto de aprovação: `{movie_ids: number[]}` SUBSTITUI o conjunto inteiro do usuário na sessão (nunca merge), array vazio limpa (operação válida). Três achados que uma port "limpa" erraria:
  - ⚠️ **A ORDEM real entre `not_authenticated` (401) e `invalid_id` (400) não se lê só em `CreateVote`** (`apps/api/internal/handlers/votacao/votes.go`) — o handler tem um `user == nil` redundante DEPOIS do `parseID`, mas `router.go` monta a rota atrás de `r.With(auth.RequireAuth(...))`, MIDDLEWARE que roda ANTES do corpo do handler em qualquer request real. A ordem OBSERVÁVEL via HTTP é 401 primeiro, sempre — o check interno é código morto pela rota montada, só alcançável chamando o handler direto (é assim que `votes_test.go` o exercita, por isso aquele teste não prova a ordem real). `requireAuth<AuthBindings>()` aqui é montado como middleware Hono, mesma topologia, mesma convenção já usada por `GET /sessions/{id}` (T2).
  - ⚠️ **`'success'` voltou a `NotificationKind`** (`src/lib/envelope.ts`) — T1/T2 tinham omitido de propósito ("especulativo"), mas o Go usa `httpx.DataMsg(..., httpx.Success("Voto registrado."))` aqui, e `apps/web/lib/votacao/api-client.ts` já tipava `ApiNotification.type` incluindo `'success'` há tempo. `okJson` ganhou um terceiro parâmetro opcional (`notifications`, default `[]`) — todo chamador existente continua idêntico. É paridade de CONTRATO, não uma tela nova: `call<T>()` do `apps/web` descarta `notifications` no caminho feliz.
  - ⚠️ **Teto de 100 bound params por statement no D1** — `votes` tem 3 colunas bound (`session_id`, `user_id`, `movie_id`) ⇒ 33 linhas/statement. Uma sessão real nunca chega perto disso, mas o chunking está escrito e testado com um lote de 70 ids (`domain/votes.test.ts`) — mesmo padrão de `apps/financas/src/domain/installments.ts`.
  - Dedupe de `movie_ids` ANTES de gravar (evita 500 por `UNIQUE(session_id,user_id,movie_id)` — divergência INTENCIONAL do Go, que não dedupa e quebraria nesse cenário) e validação de pertencimento via `SELECT id FROM session_movies WHERE session_id=?` ANTES do `db.batch()` (nunca deixa a FK estourar; rejeição não é destrutiva — votos existentes sobrevivem a um replace rejeitado).
- **T4** — apuração: `GET /votacao/sessions/{id}/results` e `POST /votacao/sessions/{id}/close` (admin). `src/domain/tally.ts` (NOVO, puro — `tallyVotes`/`computeTopMovies`, porte 1:1 de `apps/api/internal/votacao/results.go`) + `src/domain/votes.ts` estendido (`listVoteMovieIds`, `countVoters`, `closeVotingSession`, `setSessionWinner` — todos I/O contra o D1, ficam em `votes.ts` e não em `sessions.ts` porque este é SÓ-leitura de propósito e `closeVotingSession`/`setSessionWinner` são ESCRITAS) + rotas em `src/routes/votacao.ts`. Cinco achados de paridade que uma port "limpa" erraria:
  - ⚠️ **`/results` ordena por `count` DESC, DEPOIS `movie_id` ASC** (`votes.go:144-150`, bubble sort manual no Go) — a rota reimplementa a MESMA chave dupla via `Array#sort`. Testado com um caso que só passa com as duas chaves certas: um filme no topo sem ambiguidade (prova a chave primária) + dois filmes empatados com o de MAIOR id votado ANTES do de MENOR id (prova que a chave secundária não é "ordem de leitura/inserção" disfarçada — `routes/votacao.test.ts`, describe `GET .../results`).
  - ⚠️ **`total_votes` (linhas de `votes`) e `total_voters` (`COUNT(DISTINCT user_id)`) DIVERGEM sob voto de aprovação** — 1 pessoa aprovando 3 filmes é `total_votes:3`, `total_voters:1`. Um teste onde os dois batem (1 voto por pessoa) não prova nada; o teste real força a divergência.
  - ⚠️ **`GET /results` NÃO checa se a sessão existe** — `parseID` valida só o FORMATO do id (`<=0` ⇒ 400 `invalid_id`, igual `POST /votes`, diferente de `GET /sessions/{id}` que aceita `id<=0`); um id positivo mas inexistente cai direto no SELECT de votos vazio e devolve **200** com `results:[]`, `total_votes:0`, `total_voters:0` — nunca 404. Reproduzido de propósito.
  - ⚠️ **`POST /close` também não distingue os casos por trás de 404 `session_not_open`** — o `UPDATE ... WHERE id=? AND status='open'` (`closeVotingSession`) devolve `false` (nenhuma linha afetada) tanto pra id inexistente quanto pra sessão já fechada; a rota responde 404 `session_not_open` pros dois, SEM uma query de existência extra pra diferenciar. Ambiguidade herdada do Go, não um defeito.
  - ⚠️ **Empate deixa `winner_movie_id` NULL** — só grava vencedor quando `computeTopMovies` devolve exatamente 1 id no topo. Quando há vencedor, um SEGUNDO `UPDATE` grava `winner_method='votes'` (`setSessionWinner`), espelhando os dois passos do Go (`CloseVotingSession` + `SetSessionWinner` chamados em sequência, não fundidos). `POST /close` **NÃO dispara backup** — o `Backuper.Run(ctx, "session_close")` do Go (goroutine) não tem equivalente aqui; o D1 usa outro mecanismo (`scripts/backup-d1.sh`), não ligado a este caminho. Mutação obrigatória do brief (fazer `close` escolher o menor id no empate) rodada e revertida — quebrou exatamente o teste de empate, confirmando que ele prova o comportamento certo.
- **T5** — `POST /votacao/sessions/{id}/tiebreak` (admin): o desempate auditável/recomputável (M8, terminologia corrigida na segunda leva de revisão — ver abaixo; era chamado de "provably-fair" aqui), **bit a bit** (mesma entrada ⇒ mesmo vencedor no Go e no ramielle, não "um sorteio justo qualquer"). `src/domain/tiebreak.ts` (NOVO, puro — `tiebreakSeed`/`pickTiebreakIndex`, porte 1:1 de `apps/api/internal/votacao/tiebreak.go`) + `src/domain/votes.ts` estendido (`createTiebreak`, escreve a linha de auditoria em `tiebreaks` — I/O, mesmo racional de `closeVotingSession`/`setSessionWinner` na T4) + rota em `src/routes/votacao.ts`. Vetor dourado gerado rodando o Go de verdade duas vezes (`apps/api/cmd/paritytmp/main.go`, descartável, apagado nas duas — `git status --porcelain` limpo confirmado nas duas rodadas): um com `serverNonce` de 16 bytes (`go-parity.json#tiebreak`, testa as funções puras isoladas) e um SEGUNDO com 32 bytes (`#tiebreak.routeScenario`, o tamanho real que a rota gera via `crypto.getRandomValues` — usado no teste de rota, que MOCKA `crypto.getRandomValues` pra devolver esse nonce exato e confere que o `winner_movie_id` da resposta bate com o que o Go calculou pra aquela combinação). Achados que uma port "limpa" erraria:
  - ⚠️ **`tiebreakSeed` ordena os `tiedIds` ASCENDENTE antes de escrever no hash** (`sort.Slice` no Go) — o seed independe da ordem de entrada. Mutação obrigatória do brief (remover o sort) rodada e revertida: quebrou exatamente o teste do vetor dourado, que usa `tiedIds` fora de ordem (`[42, 7, 19]`) de propósito.
  - ⚠️ **`pickTiebreakIndex` usa `Number`, nunca operador bitwise, pro cálculo do `limit`** (`Math.floor(2**32/n)*n`) — o Go mantém `limit` em `uint64` DE PROPÓSITO (comentário no próprio Go): pra `n` potência de 2 (`n=8`, incluído no vetor por isso), `limit` vale EXATAMENTE `2^32`, e `|`/`>>>`/`<<` do JS truncariam pra 32 bits com sinal, dando `limit=0` e um loop infinito. `2**32` é seguro como `number` (bem abaixo de 2^53) — não precisa de `BigInt` pra isso, só evitar bitwise.
  - ⚠️ **TS 5.7+ tornou `Uint8Array` genérico sobre o buffer que o apoia, com default `ArrayBufferLike`** — `crypto.subtle.digest` (WebCrypto) pede `ArrayBufferView<ArrayBuffer>`; um `Uint8Array` "normal" (bare, sem `<ArrayBuffer>` explícito) deixa de compilar contra `digest(...)`. `domain/tiebreak.ts` define um alias local `type Bytes = Uint8Array<ArrayBuffer>` e anota todo array de bytes do arquivo com ele — não é mudança de runtime, só precisão de tipo pra um conflito de default entre a lib do TS e o `worker-configuration.d.ts` do Cloudflare.
  - ⚠️ **`winner = tied[idx]` só bate com o Go se `tied` for a MESMA lista, na MESMA ordem** — `tied` vem de `computeTopMovies(votes).ids` (ordenado ASC, igual ao `ComputeTopMovies` do Go em `results.go`); `tiebreakSeed` reordena os ids só internamente pro HASH, nunca muda a ordem de `tied` usada pra indexar depois.
  - `crypto.getRandomValues` É espiável em teste (`vi.spyOn(globalThis.crypto, 'getRandomValues')`, confirmado por sondagem antes de escrever o teste de rota) — mas o Better Auth também chama `crypto.getRandomValues` internamente (hash de senha, token de sessão) durante o mesmo request/setup de teste, com tamanhos de buffer diferentes de 32. O mock do teste de rota faz login ANTES de instalar o spy, e o spy só intercepta arrays de EXATAMENTE 32 bytes (o tamanho que a rota pede), delegando qualquer outro tamanho pro `getRandomValues` original — sem esse guard por tamanho, `array.set(nonceEsperado)` estoura `RangeError: offset is out of bounds` quando o Better Auth pede um buffer menor no meio do mesmo teste.
  - Ordem das 7 checagens (`invalid_id` → `invalid_json` → `invalid_entropy` → `session_not_found` → `session_not_closed` → `no_tie` 422 → `winner_already_set` 409) é a medida linha a linha em `votes.go:172-233`; `winner_already_set` só é alcançável DEPOIS de `no_tie` — testado com um cenário que tem empate de verdade nos votos ATUAIS e `winner_movie_id` já gravado por fora (simula um sorteio anterior), provando que a rota recomputa o empate mesmo quando já existe vencedor, não usa o campo como atalho.

- **T6** (esta, ÚLTIMA da fatia) — `GET /votacao/sessions/{id}/votes` (admin): o JOIN `votes ⋈ users ⋈ session_movies` que devolve quem votou em quê. `src/domain/votes.ts` estendido (`listSessionVotesWithUsers`, I/O puro — porte 1:1 de `Store.ListSessionVotesWithUsers`, `apps/api/internal/votacao/votes.go:99-127`) + rota em `src/routes/votacao.ts`. Três achados/decisões desta task:
  - ⚠️ **Esta é a ÚNICA rota da API inteira que liga e-mail de pessoa a voto** — nenhuma outra devolve `user_email`/`user_name` (nem `GET /sessions/{id}`, nem `/results`). `requireAdmin` aqui não é detalhe de implementação: é a única barreira entre o e-mail de quem votou e qualquer sessão válida. Testado com o 403 (conta não-admin) escrito e verificado ANTES do caminho feliz, e confirmado por mutação (trocar `requireAdmin` por `requireAuth` na rota derruba exatamente esse teste — ver `routes/votacao.test.ts`, describe `GET /votacao/sessions/:id/votes`).
  - **A resposta não passa por `lib/wire.ts`** — diferente de `sessionToWire`/`movieToWire`, todo o shape aqui já é snake_case (`user_id`, `user_name`, `user_email`, `movie_id`, `movie_title`, `category`, `created_at`), porque o handler Go monta um `map[string]any` de chaves explícitas (não faz `json.Marshal` de uma struct PascalCase, ao contrário de `VotingSession`/`SessionMovie`). O domínio devolve `created_at` CRU (formato D1); a ROTA aplica `toIsoUtc` na montagem final — mesma divisão de responsabilidade que `sessionToWire`/`movieToWire` já seguem, só que sem struct intermediária porque não existe uma do lado Go.
  - **Mesma família de `/results` (T4): não checa se a sessão existe.** `ListSessionVotes` do Go só valida o FORMATO do id (`parseID`) e vai direto pro SELECT/JOIN — um id positivo mas inexistente não bate nenhuma linha e devolve **200** com `votes:[]`, `total:0`, nunca 404.
- **Também nesta task:** `app.onError` global em `src/index.ts` (ver § _`app.onError` global_ abaixo) e a rename `parseParseIDStyle` → `parseIdDaRota` em `routes/votacao.ts` (puramente estético — "parse" duplicado no nome original, nenhuma mudança de comportamento).

**Fatia ② (votação) está COMPLETA** — as 7 rotas que só dependem do D1 (sem Sheets/TMDb) estão montadas e testadas. `apps/web` em produção continua na Go; nada muda até o cutover (fatia ④). Ver § _As duas rotas que ficaram pra fatia ③_ abaixo pro que falta pra fechar `/votacao` inteiro.

## A mistura de convenções É O CONTRATO — PascalCase × snake_case, e o vetor dourado

Toda resposta de `/votacao/*` mistura duas convenções de caso, de propósito, porque a API Go que ela reproduz também mistura:

- **PascalCase** — só `session`/`movies` em `GET /sessions/{id}` (e `sessions[]` em `GET /sessions`). Vem de `json.Marshal` das structs Go `VotingSession`/`SessionMovie` (`apps/api/internal/votacao/sessions.go:11-21`, `movies.go:9-19`) — **sem tag `json:` nenhuma**, então o Go emite o nome do campo Go literal (`ID`, `CreatedBy`, `WinnerMovieID`...). `src/lib/wire.ts#sessionToWire`/`movieToWire` reproduzem isso byte a byte.
- **snake_case** — tudo o mais: `has_voted`, `voted_movie_ids`, `movie_id`, `total_votes`, `total_voters`, `winner_movie_id`, e a resposta inteira de `GET /sessions/{id}/votes` (T6). Vem de handlers que montam a resposta com `map[string]any` de chaves explícitas, em vez de serializar uma struct.

**Não "padronizar" pra um caso só** — `apps/web/lib/votacao/types.ts` já tipa `VotingSession.ID`/`.Title` em PascalCase e o resto em snake_case; trocar quebra a tela em silêncio (`res.json()` é `any` em TS, nada denuncia em compile-time).

**O vetor dourado** (`src/routes/__fixtures__/go-parity.json`) é a prova de que `sessionToWire`/`movieToWire` reproduzem o Go byte a byte — gerado rodando o Go DE VERDADE, nunca escrito à mão a partir da leitura do código-fonte. Regenerá-lo (se as structs Go mudarem, ou pra ampliar o vetor com um novo shape):

1. `VotingSession`/`SessionMovie` moram em `internal/votacao` (pacote `internal` do Go — a regra de visibilidade é resolvida pelo **import path**, `github.com/PiluVitu/api/internal/votacao`, não pelo disco físico). Um módulo `parity` separado com `replace` no `go.mod` **não funciona**: `replace` aponta o compilador pro disco certo, mas o import path continua `parity`, que nunca fica "dentro" de `github.com/PiluVitu/api` — `go run` falha com `use of internal package ... not allowed`. Medido de verdade na T1 (`.superpowers/sdd/2026-07-29-ramielle-fatia2-votacao/task-1-report.md`, gitignored — por isso este raciocínio está aqui, não só lá).
2. **Caminho que funciona:** um arquivo `main.go` temporário DENTRO do módulo Go, em `apps/api/cmd/<nome-descartável>/main.go` (import path automaticamente `github.com/PiluVitu/api/cmd/<nome>`, autorizado a importar `internal/votacao`). Monta os valores (`VotingSession{...}`, `SessionMovie{...}`) e imprime `json.Marshal` de cada um.
3. Rodar de dentro de `apps/api`: `go run ./cmd/<nome-descartável>`.
4. Copiar a saída **literal** (sem editar um caractere) pro JSON do fixture.
5. **Apagar `apps/api/cmd/<nome-descartável>/` inteiro** e confirmar `/usr/bin/git status --porcelain` limpo em `apps/api` antes de tocar em qualquer outro arquivo — nenhum resíduo do experimento pode entrar no diff da API Go (este workspace só PORTA a Go, nunca a modifica). Feito duas vezes nesta fatia (T1: `sessionAberta`/`sessionFechada`/`filmeSemTmdb`/`filmeComTmdb`; T5: os dois cenários de `tiebreak`, com nonces de 16 e 32 bytes) — as duas com o `git status` limpo confirmado.

## `created_at`/`CreatedAt`: por que passa por `toIsoUtc`

O Go serializa `time.Time` em RFC3339 (`2026-05-19T12:00:00Z`). O D1 grava a coluna como `TEXT DEFAULT CURRENT_TIMESTAMP`, e o `CURRENT_TIMESTAMP` do SQLite escreve `2026-05-19 12:00:00` — **espaço** como separador, sem `T`, sem `Z`. Devolver esse valor cru quebra o cliente: `new Date('2026-05-19 12:00:00')` é aceito pelo V8 (Node/Chrome) e **rejeitado pelo Safari** (`Invalid Date`) — mesma classe de armadilha de fuso/parsing que só aparece em um dos ambientes, já medida três vezes neste projeto.

`src/lib/dates.ts#toIsoUtc` normaliza qualquer variação de formato pro RFC3339 completo antes de sair no fio; `nowIsoUtc` gera timestamps novos já nesse formato (usado por `closeVotingSession`, nunca `new Date()` cru dentro do domínio). Aplicado em `sessionToWire`/`movieToWire` (`CreatedAt`/`ClosedAt`) e, desde a T6, também na rota `GET /sessions/{id}/votes` (`created_at`) — ali direto na rota, não no domínio, porque `listSessionVotesWithUsers` é I/O puro e a normalização de fio é responsabilidade de quem monta a resposta.

## As duas rotas que ficaram pra fatia ③ — `/categorias` e `POST /sessions`

O Go tem **9** rotas em `/votacao` (`router.go:69-77`); a fatia ② implementou **7**. As duas de fora — `GET /categorias` e `POST /sessions` — dependem de `h.deps.Sheets` (Google Sheets):

- **`GET /categorias`** (`handlers/votacao/categorias.go`) chama `h.deps.Sheets.GetCategories(ctx)` — lê a aba de categorias da planilha de filmes. **Entregue na fatia ③, Task 2** — ver § _Sheets — cliente e `GET /categorias` (fatia ③ Task 2)_ abaixo.
- **`POST /sessions`** (`handlers/votacao/sessions.go:30-`) chama `h.deps.Sheets.ReadMovies(ctx)` — lê a lista de filmes candidatos da planilha, sorteia 1 por categoria, enriquece com TMDb, grava `voting_sessions`+`session_movies`. **Entregue na fatia ③, Task 4** — ver § _TMDb e `POST /votacao/sessions` (fatia ③, Task 4)_ abaixo. As **9** rotas do Go estão agora todas portadas.

As duas respondem `503 sheets_disabled` no Go quando `h.deps.Sheets == nil` — ou seja, mesmo a Go trata isso como dependência OPCIONAL/desligável, não uma garantia.

## Sheets — cliente e `GET /categorias` (fatia ③, Task 2)

`src/lib/gsheets.ts` — porte de `apps/api/internal/gsheets/movies.go` (`ReadMovies`/`GetCategories`/`parseRow`), **sem SDK do Google** (`googleapis`): troca a service account (`lib/google-auth.ts#getAccessToken`, T1 desta fatia) por um access token OAuth2 com escopo `https://www.googleapis.com/auth/spreadsheets.readonly`, e lê a API REST v4 (`GET /v4/spreadsheets/{id}/values/{range}`) direto via `fetch`. `GET /votacao/categorias` (`routes/votacao.ts`) monta a config a partir de três bindings novas (`GOOGLE_SA_JSON`, `GSHEETS_MOVIES_SPREADSHEET_ID`, `GSHEETS_MOVIES_RANGE`, todas em `AuthBindings`/`lib/auth.ts`) e chama `getCategories`.

- ⚠️ **O achado que decide o arquivo inteiro, medido contra a planilha real (2026-08-06): a categoria é a coluna D (índice 3), NÃO a B.** Layout: `A=Nº, B=Título, C=Filme/Série, D=Gênero, E=Assistido?, F=Nota` (mesmo comentário de `gsheets/movies.go:52`). Um spike que leu o índice 1 (título) como categoria produziu 255 "categorias" que na verdade eram títulos de filme — a lista PARECIA plausível; só comparando com o Go (11 categorias reais) o erro aparecia. Fixado por mutação: trocar o índice 3 pelo 1 em `parseRow` derruba 6 testes de `gsheets.test.ts` (o da linha real da planilha incluso) — rodada e revertida, árvore confirmada limpa.
- `parseRow`: `row.length < 5` descarta (larguras observadas: 5 e 6); título vazio (trim) OU categoria vazia (trim+lowercase) descarta; `number` (coluna A) é TOLERANTE — mimica `strconv.Atoi` do Go IGNORANDO o erro (lixo/vazio vira `0`, nunca descarta a linha); `type` é `'serie'` quando o cru é `serie` OU `série` (a planilha real usa a forma ACENTUADA), senão `'filme'`; `watched` é `true` só para `sim`/`yes`/`true`/`1` (case-insensitive). 745 de 1001 linhas da planilha real são descartadas (256 usáveis) — NORMAL, não defeito; fixado por um teste dedicado (`readMovies`, 4 linhas de entrada, 1 sobrevive).
- ⚠️ **`getCategories` ordena por BYTE/CODEPOINT, não `localeCompare`** — o Go usa `slices.Sort` sobre `string` (ordem de byte UTF-8). `Array#sort()` sem comparador (usado aqui) compara por unidade de código UTF-16, que pra todo caractere do BMP (inclusive os acentuados das categorias reais) produz a MESMA ordem relativa que bytes UTF-8 — propriedade da própria codificação. Testado com as 11 categorias reais medidas: `animação, aventura, ação, comédia, crime / investigação, documentário, drama, ficção científica, romance, suspense, terror` — essa ordem SÓ passa com comparação de byte/codepoint (`localeCompare('pt-BR')` colateria `ç`~`c` e produziria `ação` primeiro, MEDIDO).
- **Config**: `GOOGLE_SA_JSON` (o JSON inteiro da service account), `GSHEETS_MOVIES_SPREADSHEET_ID`, `GSHEETS_MOVIES_RANGE` (default `'A2:F'`, aplicado em `routes/votacao.ts` quando ausente/vazia — mesmo fallback do Go, `cmd/api/main.go:63-65`). As três em `AuthBindings` (`lib/auth.ts`) — vivem lá pelo mesmo motivo de `CORS_ALLOWED_ORIGINS`: `AuthBindings` é, na prática, o tipo `Bindings` inteiro do Worker. **Nenhuma das três em `wrangler.jsonc#vars`** — ⚠️ decisão de quem implementou a task, não pedido do `task-2-brief.md` (que não cita `wrangler`/`vars`/`Bindings`/`.dev.vars`/`secret` em lugar nenhum, `grep` confirma). Só `GOOGLE_SA_JSON` é literalmente secreta; `GSHEETS_MOVIES_SPREADSHEET_ID`/`RANGE` ficaram de fora de `vars` por PARIDADE com o Go, que também as mantém fora do git — `infra/docker-compose.yml` não as declara no bloco `environment:` (só `GOOGLE_APPLICATION_CREDENTIALS`, o path pro secret montado); as duas só chegam via `env_file: ../apps/api/.env`, gitignored. Fica pro dono decidir se quer comitá-las como `vars` aqui.
- **`GET /votacao/categorias`**: `200 {categories: string[]}`. `GOOGLE_SA_JSON` OU `GSHEETS_MOVIES_SPREADSHEET_ID` ausentes ⇒ `503 sheets_disabled`, `"Integração com a planilha está desativada."` (mensagem copiada byte a byte de `handlers/votacao/categorias.go`). Qualquer falha de LEITURA (troca de token, `fetch`, resposta não-ok, JSON malformado) ⇒ `502 sheets_read_failed`, `"Falha ao ler a planilha de filmes."` (o Go usa `StatusBadGateway`).
- ⚠️ **Achado além do brief, MEDIDO contra `cmd/api/main.go:66-71`**: o Go trata QUALQUER falha de CONSTRUÇÃO do cliente (`gsheets.NewClient` retornando erro — o que acontece com uma credencial ADC/service account malformada) como "sheets desligado": só LOGA e segue com `sheetsClient` permanecendo `nil`, NUNCA propaga como falha de leitura. Só chamadas que falham DEPOIS do cliente já construído (a troca de token, o fetch do range) viram 502. Por isso `GOOGLE_SA_JSON` presente mas malformado (JSON inválido ou campo obrigatório faltando) também cai em **503** `sheets_disabled` aqui — não 502. O brief só cobria "faltar" a env var; este é comportamento real do Go que o brief não descrevia. Testado (`routes/votacao.test.ts`, describe `GET /votacao/categorias`).
- **Testes nunca chamam o Google de verdade** — mesmo padrão de `lib/auth.test.ts`/`lib/google-auth.test.ts`: mocka `globalThis.fetch` interceptando só o token endpoint (SA de teste com chave RSA gerada em runtime, `crypto.subtle.generateKey`) e o endpoint de values do Sheets, delegando o resto pro `fetch` original, restaurado num `finally`.
- **Rota montada ACIMA do catch-all** — `GET /votacao/categorias` entrou no `test.each` de "montagem" de `index.test.ts` (8 rotas agora, era 7).

**Contagem de testes ao fim desta task: 307** (`pnpm --filter @piluvitu/ramielle exec vitest run`, resumo do terminal — eram 247 ao fim da fatia ②). +60: `src/lib/gsheets.test.ts` (arquivo novo — `parseRow`, `readMovies`, `getCategories`), `routes/votacao.test.ts` (describe `GET /votacao/categorias`), `index.test.ts` (+1 linha no `test.each` de montagem).

## O sorteio — `domain/sortear.ts` (fatia ③, Task 3)

Porte 1:1 de `apps/api/internal/votacao/sortear.go` (`SortOnePerCategory`/`filterMovies`/`stringSet`) — função PURA, sem D1/HTTP/`Math.random` direto, usada por `POST /votacao/sessions` (fatia ③, Task 4 — ver abaixo) pra escolher 1 filme por categoria a partir do catálogo lido do Sheets. `SheetMovie` é reusado de `lib/gsheets.ts` (T2), não redefinido.

- `sortOnePerCategory(movies, opts, rng)` — `opts: SortOnePerCategoryOptions` tem os 3 campos do Go, todos OPCIONAIS (espelhando o zero-value do struct Go): `types`/`categories` ausentes ou `[]` = sem filtro; `includeWatched` ausente = `false` (só não-assistidos). `rng: Rng` é `() => number`, mesma forma de `Math.random` (devolve `[0, 1)`) — o chamador injeta o gerador, nunca `Math.random` é chamado dentro da função (garantido por teste com `vi.spyOn(Math, 'random')`).
- ⚠️ **Erro de domínio decidido olhando o padrão já usado em `domain/votes.ts`**: `MovieNotInSessionError extends Error` já é o precedente pra um erro Go sentinela (`errors.Is(err, votacao.ErrX)`) virar uma classe nomeada, checada com `instanceof` pela rota. `NoCandidatesError` (`domain/sortear.ts`) segue o MESMO padrão, não um terceiro estilo — `POST /votacao/sessions` (T4) usa `instanceof NoCandidatesError` pra decidir o `422 no_candidates` (mesma paridade do Go, `handlers/votacao/sessions.go:64-67`, `"Nenhum filme corresponde aos filtros escolhidos."`).
- ⚠️ **Categorias saem em ordem alfabética via `.sort(([a], [b]) => …)`** — comparador explícito porque o que se ordena são as tuplas `[chave, valor]` do `Map`, não strings soltas; é equivalente ao sort padrão de string (mesma comparação por unidade de código UTF-16), mesma justificativa já documentada em `lib/gsheets.ts#getCategories` (ordem de unidade de código UTF-16 = ordem de byte UTF-8 do Go pra todo caractere do BMP, acentuados inclusos). É o que torna a saída ESTÁVEL. Mutação obrigatória do brief (remover o `.sort()`) rodada: derrubou os 3 testes que dependem da ordem (`categorias saem ordenadas alfabeticamente`, e os 2 testes de `rng` que consomem a fila de valores na ordem alfabética das categorias) — os outros 9 continuaram verdes. Revertida, `/usr/bin/git diff` confirmado vazio antes do commit.
- Escolha dentro de cada grupo: `Math.floor(rng() * candidates.length)` sobre a lista já agrupada (evita um segundo `Map#get` que devolveria `SheetMovie[] | undefined`) — as entradas do `Map` são ordenadas (`[...byCategory.entries()].sort(...)`) ANTES de mapear, então o `rng` é consumido exatamente na mesma ordem alfabética em que os grupos aparecem na saída.
- **Testes**: os 9 casos de `sortear_test.go` (happy path, filtro por type, exclude/include watched, filtro por categories, sem candidatos ×2, determinismo com seed, ordenação alfabética) mais 4 específicos de TS — um PRNG determinístico (`mulberry32`) pra provar que duas chamadas com a mesma seed produzem a mesma saída, dois `rng` de fila (`queueRng`) de valor CONSTANTE pra provar a escolha dentro do grupo (primeiro/último candidato), e o spy em `Math.random` citado acima.
- ⚠️ **Um `queueRng` de valor constante NÃO prova a ordem de consumo** (achado Minor da revisão da T3, corrigido): com valor constante, a ordem em que `rng()` é chamado é inobservável — qualquer ordem devolveria o mesmo número, e os dois testes só provavam a ordem de SAÍDA. O teste que prova a alegação usa valores DISTINTOS (`queueRng([0, 0.9, 0])`, `includeWatched:true`): ordem alfabética dá `[John Wick, Forrest Gump, A Coisa]`, ordem de inserção daria `Breaking Bad` no drama. É esse par que discrimina.

**Contagem de testes depois desta task: 322** (`pnpm --filter @piluvitu/ramielle exec vitest run --reporter=json`, campo `numTotalTests`) — eram 309 antes. +12: `src/domain/sortear.test.ts` (arquivo novo).

## TMDb e `POST /votacao/sessions` (fatia ③, Task 4)

`src/lib/tmdb.ts` — porte de `apps/api/internal/tmdb/search.go#SearchPoster`, sem SDK (`fetch` puro, mesmo desenho de `lib/gsheets.ts`/`lib/google-auth.ts`). `searchPoster(cfg, title, mediaType, opts?)`: `GET /3/search/movie` ou `/3/search/tv` (`mediaType 'serie'` ⇒ `tv`). **Fail-soft é o ponto inteiro do arquivo**: `404` ou `results` vazio devolvem `{posterUrl:'', tmdbId:0}` — nunca erro; só `5xx`, `4xx≠404`, falha de rede ou JSON quebrado lançam. `poster_path` nulo/vazio também é fail-soft, mas PRESERVA o `tmdbId` real (`{posterUrl:'', tmdbId: id}}` — o Go faz o mesmo, `r.PosterPath == nil` não zera `r.ID`). `TmdbConfig.baseUrl` é parametrizável só pra teste (mesmo papel do `NewClientWithBase` do Go).

`src/domain/sessions.ts` ganhou `createVotingSession` (INSERT + `RETURNING id` + reuso de `getVotingSession`, mesmos dois passos do Go) e `insertSessionMovies` (porte de `Store.InsertSessionMovies`, chunking em `db.batch()`). `src/routes/votacao.ts` ganhou `POST /sessions` (admin) e um helper `resolverSheetsConfig` (extraído de dentro de `GET /categorias`, que passou a chamá-lo também — refactor puro, nenhum comportamento mudou).

- ⚠️ **A ORDEM real das checagens diverge do resumo do brief desta task, medida linha a linha em `handlers/votacao/sessions.go:30-51`: o Sheets é checado ANTES de decodificar o corpo, não depois.** `CreateSession` (Go) testa `h.deps.Sheets == nil` como a PRIMEIRA linha da função — antes até do `user == nil` (código morto, o guard real é o middleware `RequireAdmin`, mesma topologia de `POST /close`/`POST /tiebreak`) e bem antes de `json.NewDecoder(r.Body).Decode(&body)`. Consequência OBSERVÁVEL: um `POST /votacao/sessions` com corpo JSON quebrado E Sheets desligado responde `503 sheets_disabled`, nunca `400 invalid_json` — testado (`routes/votacao.test.ts`, "Sheets desligado responde 503 ... MESMO com corpo JSON quebrado").
- ⚠️ **`title` vazio NÃO é `invalid_json` — é um código e campo PRÓPRIOS.** O Go usa `httpx.Errors` (não `httpx.Error`) com `Code: "title_required"`, `Field: "title"`, mensagem `"Informe um título para a sessão."`. O brief resumia os dois num único passo ("corpo não-JSON OU title vazio ⇒ 400 invalid_json") — comportamento real do Go, não o do brief, foi implementado: dois códigos distintos, e o segundo carrega `field`. Também NÃO faz `trim()` — um título só de espaços passa direto dessa checagem (mesma paridade, `body.Title == ""` cru no Go), testado.
- **Ordem completa das checagens dentro do handler**: `0` (middleware) `401`/`403` → `1` Sheets desligado/malformado `503 sheets_disabled` → `2` corpo não-JSON `400 invalid_json` → `3` título vazio `400 title_required` → `4` falha de leitura do Sheets `502 sheets_read_failed` → `5` sem candidatos `422 no_candidates` → `6` (defensivo, inalcançável — `sortOnePerCategory` só lança `NoCandidatesError` na prática) `500 internal_error`.
- **TMDb em paralelo, teto de concorrência 5, timeout 3s/busca** — `fetchPostersConcorrente` (privada, `routes/votacao.ts`) usa um semáforo PRÓPRIO (`criarSemaforo`, fila FIFO, sem dependência nova) + `AbortController` por busca. `Promise.all` (não `allSettled`) é seguro porque cada tarefa mapeada tem seu PRÓPRIO `try/catch` que NUNCA deixa a promise rejeitar — é esse catch, e não o combinator, que garante o fail-soft. Testado com 7 categorias (acima do teto) e delay controlado em cada busca mockada: pico de chamadas EM VOO medido em exatamente 5, nunca mais.
- **Mutação obrigatória (Step 5) rodada e revertida**: remover o `catch` de `fetchPostersConcorrente` (deixando o erro do TMDb propagar por `Promise.all`) derruba exatamente o teste "uma busca do TMDb que FALHA não derruba a sessão" — a resposta vira `500 internal_error` (via `app.onError` global) em vez de `201`. `/usr/bin/git diff` confirmado vazio depois de reverter.
- **Sem `TMDB_API_KEY`, a sessão é criada SEM pôsteres — nunca erro** (`c.env.TMDB_API_KEY` ausente pula `fetchPostersConcorrente` inteiro, mapeia `{posterUrl:'', tmdbId:0}` pra todo filme; testado que o TMDb mock recebe ZERO chamadas nesse caso, não só que os campos saem vazios).
- **`session_movies`: 8 colunas bound ⇒ 12 linhas/statement** (`session_id, category, title, type, poster_url, tmdb_id, was_watched, sheet_number`; `id` é AUTOINCREMENT, nunca bound) — mesmo padrão de `voteInsertStatements` (`domain/votes.ts`). Com as ~11 categorias reais isto nunca chunka na prática; testado mesmo assim com um lote de 25 filmes (`domain/sessions.test.ts`, describe `insertSessionMovies`), confirmando 1 único `db.batch()` com 3 statements dentro (`ceil(25/12)`).
- ⚠️ **`insertSessionMovies` NÃO inclui o INSERT da sessão no mesmo `db.batch()`** — divergência deliberada da leitura mais literal do brief ("a sessão e seus filmes, ou nada" sugere um batch só). Duas razões: (1) o Go TAMBÉM não é atômico entre os dois — `CreateVotingSession` é um INSERT isolado, só `InsertSessionMovies` tem sua própria transação; (2) tecnicamente arriscado — **não há `last_insert_rowid()` confiável entre statements de um `db.batch()` no D1** (achado MEDIDO documentado em `apps/financas/src/lib/ids.ts:6`, motivo pelo qual o finanças usa PK `TEXT`/UUID em vez de `INTEGER` — ver o comentário daquele arquivo, não a seção "decisão da PK `INTEGER`" acima, que é sobre o motivo OPOSTO, o ramielle ter mantido `INTEGER`), então referenciar o id da sessão recém-criada DENTRO do mesmo batch não é um mecanismo testado/seguro aqui. `db.batch()` cobre só o CONJUNTO de filmes (todos os chunks, ou nenhum) — se falhar, a sessão fica órfã (sem filme nenhum), mesmo comportamento observável do Go nesse cenário de falha.
- **`TMDB_API_KEY` — nova binding opcional em `AuthBindings`** (`lib/auth.ts`), mesmo padrão das três do Sheets: `undefined` é o estado normal até `wrangler secret put TMDB_API_KEY` (pendência do dono, ver a lista no fim deste arquivo). Nunca em `wrangler.jsonc#vars` (é secreta de verdade).
- **Dívida técnica resolvida (decisão registrada no ledger)**: o helper de mock do Sheets (gerar service account de teste + instalar `fetch` mockado pro token/values endpoint) estava duplicado 3× (`lib/gsheets.test.ts`, e dois `describe`s em `routes/votacao.test.ts`). Extraído pra `src/test-support/gsheets-mock.ts` (`gerarServiceAccountDeTeste`/`instalarMockGoogleSheets`, esta última unificando as duas variantes anteriores — a simples de 2 cópias e a de controle total usada pelos testes de vazamento de segredo) — as 3 cópias migradas, nenhuma lógica de teste mudou (`git diff` de comportamento de teste é zero, só de organização). `POST /votacao/sessions` reusa o mesmo helper (seria a 4ª cópia se não tivesse sido extraído).
- **Segredo NUNCA vaza — mesmo padrão estendido pra `TMDB_API_KEY`**: como a chave vai na query string da busca (`lib/tmdb.ts`), toda mensagem de erro lançada por `searchPoster` é um texto FIXO — nunca a URL, nunca o erro cru do `fetch`/`.json()` (que em alguns runtimes ecoa a URL). Testado em dois níveis: `lib/tmdb.test.ts` (describe "TMDB_API_KEY NUNCA vaza", unitário, incluindo um cenário onde o erro de rede SIMULADO já cita a URL na própria mensagem — prova que `searchPoster` não repassa isso) e `routes/votacao.test.ts` (describe implícito dentro de `POST /votacao/sessions`, ponta a ponta: TMDb falha, sessão é criada mesmo assim via fail-soft, e nem a resposta nem o `console.error` contêm a chave-marcador).

**Contagem de testes depois desta task: 361** (`pnpm --filter @piluvitu/ramielle exec vitest run --reporter=json`, campo `numTotalTests`) — eram 322 antes. +39: `src/lib/tmdb.test.ts` (arquivo novo, 17 testes), `src/domain/sessions.test.ts` (+8: `createVotingSession` ×3, `insertSessionMovies` ×5 incluindo o chunking e o rollback de `UNIQUE`), `src/routes/votacao.test.ts` (+13 no describe `POST /votacao/sessions`), `src/index.test.ts` (+1 entrada no `test.each` de montagem — nenhuma linha `test(`/`it(` nova, é o array que ganhou a 9ª rota do Go, agora completo).

## `/admin` — as 9 rotas do Go completas (fatia ③, Task 5, ÚLTIMA task de código da fatia)

Porte de `apps/api/internal/handlers/admin/{users,backup}.go` (montagem em `router.go:81-85`, todas atrás de `RequireAdmin`) — `GET /admin/users`, `GET /admin/backups`, `POST /admin/backup`. `src/domain/admin.ts` (`listUsers`/`listBackups`, NOVO) + `src/routes/admin.ts` (as três rotas, NOVO). Com esta task, **as 9 rotas do Go inteiro estão portadas** — as 7 da fatia ② mais `/categorias`/`POST /sessions` da fatia ③ (T2/T4) mais estas 3.

- ⚠️⚠️ **A ARMADILHA DESTA TASK, MEDIDA linha a linha contra `users.go`/`backup.go`: as duas rotas de LEITURA têm convenções de wire OPOSTAS.**
  - `GET /admin/users` é **snake_case** (`is_admin`, `created_at`) — o handler Go monta um `map[string]any` explícito, à mão. `domain/admin.ts#listUsers` já devolve o shape FINAL nesse formato — `google_sub` NUNCA sai (a query nem seleciona a coluna).
  - `GET /admin/backups` é **PascalCase** — o handler Go devolve `[]Backup` DIRETO (`httpx.Data(w, 200, map[string]any{"backups": rows})`), e o struct `Backup` (`internal/votacao/backups.go:11-18`) NÃO tem tag `json:` nenhuma, então o encoder usa os nomes de campo Go como estão. `lib/wire.ts` ganhou `BackupRow`/`WireBackup`/`backupToWire` — MESMO precedente de `sessionToWire`/`movieToWire`, nunca montado à mão na rota.
  - O vetor dourado (`routes/__fixtures__/go-parity.json#backup`) foi gerado rodando o Go de verdade (`apps/api/cmd/paritybackup/main.go`, descartável — apagado depois de capturar a saída, `/usr/bin/git status --porcelain` confirmado limpo em `apps/api` antes de tocar em qualquer outro arquivo, mesma disciplina de T1/T5 da fatia ②). `wire.test.ts` ganhou um describe `backupToWire` com uma asserção de chaves PascalCase LITERAL — emitir `drive_file_name` em vez de `DriveFileName` quebraria `/admin/sessoes` (`apps/web`, `components/votacao/admin/backups-panel.tsx`) em produção, tabela renderizando `undefined` em toda linha, sem erro nenhum.
- **`ORDER BY created_at DESC, id DESC` nas duas** (`users.go:52`/`backups.go:56`) — portado em `domain/admin.ts`, com o mesmo cuidado já medido na T3 da fatia ②: os testes de desempate usam linhas com `created_at` IGUAL e `id` diferente, não valores distinguíveis por acidente.
- ⚠️ **C1/I1 (fix round 1 da revisão) — `listUsers` tinha DUAS lacunas de paridade de dados, e a suíte tinha travado as duas como se fossem certas.** `domain/admin.ts` devolvia `created_at`/`picture` CRUS do D1 (`"2026-05-19 12:00:00"`, `null`); os testes fixavam esses dois valores como esperados, então a suíte estava verde EM CIMA do defeito. Corrigido: `created_at` agora passa por `toIsoUtc` (`"2026-05-19T12:00:00Z"` — o Go serializa `User.CreatedAt` via `json.NewEncoder`, RFC3339; o formato cru é aceito pelo V8 e **rejeitado pelo Safari**, `Invalid Date`, mesma classe de bug já medida 3× neste projeto) e `picture` vira `row.picture ?? ''` (o Go declara `votacao.User.Picture string`, nunca ponteiro — `sql.NullString.String` vira `""`; MESMO campo que `routes/auth.ts#GET /auth/me` já normaliza do mesmo jeito). `GET /admin/backups` (`backupToWire`) já fazia a coisa certa desde o começo — as duas rotas desta task agora emitem o MESMO formato RFC3339 pra `created_at`, lido da MESMA forma de coluna D1. Nenhuma outra rota já portada no resto do Worker emite data crua (`routes/votacao.ts` já aplicava `toIsoUtc` em `GET /sessions/{id}/votes` desde a T6 da fatia ②; `lib/wire.ts#sessionToWire`/`movieToWire` idem) — `domain/users.ts#VotacaoUser.createdAt` é lido mas NUNCA serializado em resposta nenhuma (`GET /auth/me` o exclui de propósito, ver `routes/auth.ts`). Verificado por mutação: remover o `toIsoUtc` de novo derruba exatamente os 2 testes que fixam o formato RFC3339 (`domain/admin.test.ts` e `routes/admin.test.ts`); revertido, `/usr/bin/git diff` confirmado idêntico à correção antes do commit.
- **Minor da mesma revisão — fronteira do clamp fixada.** O teste de clamp usava só `0, -5, 500, 10`, sem exercitar a fronteira exata de `limit > 200`. Acrescentados `200` (não clampa, devolve as 200 linhas) e `201` (clampa pro default 50) — é onde um off-by-one (`>= 200` no lugar de `> 200`) moraria sem ser pego.
- **`listBackups` porta o CLAMP do Go, não só o "sempre 50"**: `if limit <= 0 || limit > 200 { limit = 50 }` (`backups.go:51`) — a rota sempre chama com `50`, então o clamp nunca dispara na prática, mas está testado pra qualquer valor (`domain/admin.test.ts`, describe `listBackups`).
- **`POST /admin/backup` é SEMPRE `503 backup_disabled`** — não é um stub preguiçoso, é o MESMO caminho degradado que a Go tem quando `h.deps.Runner == nil` (nenhum `GDRIVE_BACKUP_FOLDER_ID` configurado), e `apps/web` já trata esse código. Aqui o degradado é **permanente**, não uma pendência de configuração futura: o D1 não tem `VACUUM INTO` (a técnica que `backup.Runner.Run` usa pra gerar o snapshot SQLite antes de subir pro Drive) — o backup deste monorepo é outro mecanismo inteiramente (`scripts/backup-d1.sh`, export lógico via `wrangler d1 export`), não ligado a este endpoint.
- ⚠️ **Achado desta task: a tabela `backups` já existia** — `migrations/0001_votacao.sql` (T2 da fatia ②) já criou `backups` + `idx_backups_created` no schema completo portado do Go, então **nenhuma migration nova foi necessária** aqui (o brief desta task cogitava a possibilidade de precisar criar a tabela; medido contra o schema real antes de escrever qualquer SQL, e ela já estava lá).
- **Mutação obrigatória (Step 4) rodada e revertida**: trocar `requireAdmin` por `requireAuth` em `POST /admin/backup` faz o teste "conta autenticada mas não-admin responde 403 admin_only" falhar (recebe `503` em vez de `403` — a conta não-admin passa a atravessar o guard fraco e bate direto no caminho sempre-503) — confirmado rodando `vitest run src/routes/admin.test.ts` com a mutação aplicada. Revertida; `/usr/bin/git diff -- apps/ramielle/src/routes/admin.ts` confirmado vazio antes do commit (arquivo é NOVO nesta task, então o diff relevante é contra o estado já commitado deste mesmo arquivo, não contra HEAD).

**Contagem de testes depois desta task: 393** (após o fix round 1 da revisão — era 392 na entrega original; `pnpm --filter @piluvitu/ramielle exec vitest run --reporter=json`, campo `numTotalTests`) — eram **363** antes da task (medido via `/usr/bin/git stash -u` + rerun, não os "361" documentados no fim da T4 acima — a contagem já tinha andado 2 números entre o fim daquela task e o início desta, mesma classe de deriva que este arquivo já registrava "mudou 3× nesta fatia"). +30 no total: `src/domain/admin.test.ts` (arquivo novo, 13 testes — 12 da entrega original +1 do fix round, `created_at é RFC3339`), `src/routes/admin.test.ts` (arquivo novo, 12 testes), `src/lib/wire.test.ts` (+2, describe `backupToWire`), `src/index.test.ts` (+3 entradas no `test.each` de montagem — as 3 rotas de admin, nenhuma linha `test(`/`it(` nova).

## `app.onError` global — rede de segurança do envelope (T6)

MEDIDO nas T2–T5: sem um `app.onError` registrado, uma exceção não mapeada dentro de uma rota sobe pro handler default do **Hono**, que responde `text/plain "Internal Server Error"` — **fora** do envelope `{ok,data,notifications}` que toda outra resposta desta API usa. O `call<T>()` do `apps/web` (que sempre espera o envelope) levanta `invalid_envelope` — um sintoma sem relação nenhuma com a causa real, que só apareceria investigando `wrangler tail`.

`src/index.ts` registra `app.onError((err) => {...})`, devolvendo `errJson(500, 'internal_error', 'erro interno — tente novamente')` — a MESMA paridade de contrato que o Go já tem (`httpx.Error` sempre dentro do envelope, mesmo em erro não mapeado). O erro real vai só pro `console.error` (visível via `wrangler tail`), **nunca** pro corpo da resposta — `err.message`/`String(err)` são exatamente por onde um `D1_ERROR`/`SQLITE_*` cru vazaria (mesmo cuidado já tomado em `GET /health`/`db_down`). Testado em `index.test.ts` (describe `app.onError global`) com um DB shim que quebra só o SELECT de `voting_sessions`, disparado via `GET /votacao/sessions` (que não tem `try/catch` próprio em volta do domínio) — confirma 500 dentro do envelope, `code: internal_error`, e uma asserção NEGATIVA de que o texto cru do erro simulado (`D1_ERROR`, `disk I/O`) não aparece em lugar nenhum do JSON serializado.

## Votação LIVRE × finanças fail-closed — desenhos OPOSTOS do MESMO Better Auth

`apps/ramielle` e `apps/financas` usam a mesma versão do Better Auth (`1.6.25`), com desenhos de acesso **opostos**. Confundir os dois é o defeito mais caro possível nesta fatia — copiar um pro outro não é um ajuste fino, é trocar o propósito do módulo:

|                             | `apps/financas`                                                                                                  | `apps/ramielle`                                                                                                                 |
| --------------------------- | ---------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| Quem pode logar             | 1 e-mail só (`ALLOWED_EMAIL`, single-user)                                                                       | **qualquer conta Google** — a votação é pública, feita pra várias pessoas                                                       |
| Bloqueio no cadastro        | `databaseHooks.user.create.before` barra ANTES do primeiro `INSERT` (`apps/financas/CLAUDE.md` § _Autenticação_) | **nenhum hook** — `createAuth` (`src/lib/auth.ts`) não declara `databaseHooks` nenhum, de propósito                             |
| Formato da env de allowlist | `ALLOWED_EMAIL`: **1 e-mail só**, comparação de STRING INTEIRA (CSV não casa com nada)                           | `ADMIN_EMAILS`: **CSV de verdade** — `isAdminEmail` faz `.split(',')` + trim + lowercase por item, espelhando o Go (`apps/api`) |
| O que a env controla        | **ACESSO** — fora da lista, a pessoa nem entra                                                                   | **PRIVILÉGIO** — fora da lista, a pessoa entra e vota, só não é admin                                                           |

⚠️ **Copiar o hook de allowlist do finanças pra cá bloquearia todo mundo menos o dono e mataria a feature.** ⚠️ **Tirar o hook de lá abriria o finanças (single-user, fail-closed) pra qualquer conta Google.** A ausência do hook aqui é verificada por mutação em `src/lib/auth.test.ts`: reinserir o hook copiado do finanças derruba exatamente os 3 testes do describe "votação livre" (um e-mail fora de `ADMIN_EMAILS` completa o cadastro e grava 1 linha em `user`+`account`); os outros 20 testes do arquivo continuam verdes.

## `is_admin` é recalculado A CADA REQUEST — nunca gravado como verdade

`ADMIN_EMAILS` (CSV, `wrangler.jsonc#vars`) é lido a cada chamada, nunca no momento do cadastro. `src/lib/session.ts#resolveSession` chama `isAdminEmail(email, ADMIN_EMAILS)` a cada `requireAuth`/`requireAdmin` e passa o resultado pro `upsertVotacaoUser` (`src/domain/users.ts`), que **sempre sobrescreve** `is_admin` com o valor recebido — nunca faz merge preservando o que já estava gravado.

Consequência provada por teste (`session.test.ts`, describe "o caso que prova o desenho"): o **mesmo** cookie de sessão, com `ADMIN_EMAILS` trocado entre duas chamadas, muda de `403` pra `200` — sem novo login. Isso é diferente do `ALLOWED_EMAIL` do finanças por natureza, não só por formato: lá a allowlist também é avaliada a cada request (`decidirAcesso`, camada 2 do finanças), mas o que ela decide é ACESSO; aqui `ADMIN_EMAILS` decide PRIVILÉGIO dentro de um app que já é livre — o motivo estrutural é que o Better Auth não tem noção de allowlist fora do hook de criação, então um privilégio gravado no cadastro não acompanharia uma troca de `ADMIN_EMAILS` feita depois.

## Duas tabelas de usuário no mesmo D1

- **`user`** (singular, `migrations/0002_better_auth.sql`) — contrato da lib Better Auth. PK `TEXT`, uma linha por identidade autenticada, colunas camelCase (contrato da lib, não escolha deste projeto — mesma observação já registrada em `apps/financas/CLAUDE.md`).
- **`users`** (plural, `migrations/0001_votacao.sql`) — domínio da votação, portado 1:1 do schema Go. PK `INTEGER` (a que `votes`/`voting_sessions` referenciam via FK), `google_sub` UNIQUE, `is_admin`.

Nada na migration liga uma tabela na outra. **`src/domain/users.ts#upsertVotacaoUser` é a ponte** — chamada a cada request autenticado por `lib/session.ts`. Casa por `google_sub` (`INSERT ... ON CONFLICT(google_sub) DO UPDATE`), sempre sobrescreve `email`/`name`/`picture`/`is_admin` com o valor recebido (nunca merge com o que já estava no banco — mesma semântica do `UpsertUser` do Go), nunca toca `created_at` num update.

## A decisão da PK `INTEGER` + `RETURNING id` — medida na Task 2, não assumida

Ao contrário do finanças (PK `TEXT`/UUID via `crypto.randomUUID()` — lá porque o binding D1 devolve `INTEGER` como `Number` de 52 bits e não há `last_insert_rowid()` confiável entre statements de um `batch()`), o ramielle **manteve as PKs `INTEGER PRIMARY KEY AUTOINCREMENT`** do schema Go original. Motivo: `apps/web` já tipa `session_id`/`movie_id` como `number` (não `string`) em `lib/votacao/types.ts`, e os mocks E2E também — trocar pra UUID quebraria os dois sem necessidade. Esse motivo **não se aplica** ao finanças (schema novo, sem consumidor legado tipando `number`).

Essa decisão dependia de uma pergunta mensurável, não só de uma esperança: **`INSERT ... RETURNING id` funciona no D1?** Medido em `src/schema.test.ts` (describe "migration 0001 — RETURNING id"):

1. `INSERT INTO users (...) VALUES (...) RETURNING id` devolve um `id` tipo `number`, `> 0`.
2. Duas inserções sucessivas via `RETURNING id` devolvem ids **diferentes** entre si (autoincremento real, não um valor cacheado/repetido).

Os dois passaram — `RETURNING id` funciona no D1 local. `domain/users.ts#upsertVotacaoUser` usa exatamente esse mecanismo (`INSERT ... ON CONFLICT DO UPDATE ... RETURNING id`, seguido de um `SELECT` pra buscar as demais colunas — dois passos, não um `RETURNING` com colunas extras sobre `ON CONFLICT`, que nunca foi medido isoladamente e por isso não foi arriscado).

## `google_sub` guarda o `sub` REAL do Google — não o id interno do Better Auth

`getSession()` do Better Auth nunca expõe o claim `sub` bruto — só `sessao.user.id` (o id INTERNO da lib, TEXT PK de `user`). O `sub` de verdade fica em `account.accountId` (`providerId='google'`, migration `0002`).

`src/lib/session.ts#buscarGoogleSub` faz `SELECT accountId FROM account WHERE userId = ? AND providerId = 'google'` e usa esse valor como `google_sub`, com fallback pro `sessao.user.id` **só** quando não existe linha em `account` com `providerId='google'` (caminho que só acontece em teste, via a técnica `emailAndPassword`/`signUpEmail` — em produção o login é 100% Google, o fallback nunca dispara).

⚠️ **Por que isso importa pra valer, não é purismo de nome de coluna:** a fatia ④ (cutover) vai importar o histórico REAL de votação do SQLite da API Go, cujo `users.google_sub` já está preenchido com o `sub` do Google. Se o ramielle gravasse outro valor ali (o id interno do Better Auth, por exemplo), casar a importação por `google_sub` criaria uma **segunda linha** pra cada pessoa que já votou — usuário duplicado, votos antigos órfãos de identidade. Isto foi corrigido num fix round da Task 4 (a primeira versão gravava `sessao.user.id` puro); `domain/users.ts` documenta a decisão no topo do arquivo, apontando `lib/session.ts#buscarGoogleSub` como dono do "como resolver o valor" — não repetido em dois lugares.

## CORS com credenciais (Task 5)

`apps/web` mora em `piluvitu.com.br` (Vercel); o ramielle vai morar em `ramielle.piluvitu.com.br` — origens **diferentes**. Sem CORS explícito com credenciais, o cookie de sessão do Better Auth nunca atravessa um `fetch(..., { credentials: 'include' })` do `apps/web`.

`src/lib/cors.ts` espelha `apps/api/internal/router/router.go` (`corsOptions`/`allowedOrigins`) nas **origens**, nas **credenciais** e na **leitura de `CORS_ALLOWED_ORIGINS`**: mesmo default (`http://localhost:3333,https://piluvitu.com.br`), mesmo CSV, `credentials: true`. `wrangler.jsonc#vars.CORS_ALLOWED_ORIGINS` declara explicitamente `https://piluvitu.com.br` (só produção, sem `localhost`) — mesma paridade que `infra/docker-compose.yml` da Go já faz pro ambiente de produção dela; não é obrigatório (o default do código já cobriria sozinho), mas deixa a origem de produção explícita em vez de depender só do fallback.

⚠️ **A lista de HEADERS não tem paridade com a Go — de propósito, não descuido.** MEDIDO contra `corsOptions()` do Go: ela inclui `Accept` em `AllowedHeaders` e expõe `Link` em `ExposedHeaders`; `cors.ts` só permite `Content-Type`/`Authorization` e não expõe nenhum header de resposta. Inofensivo hoje: `Accept` é um dos headers CORS-_safelisted_ do fetch spec (não precisa estar na lista pra ser enviado), e nenhuma rota do ramielle hoje emite `Link`. Reavaliar isto quando a fatia ② (rotas de votação) existir — se alguma resposta precisar que `apps/web` leia um header customizado via JS, ele precisa entrar em `exposeHeaders`, que hoje é vazio.

⚠️ **`Access-Control-Allow-Origin: '*'` é incompatível com credenciais** — o navegador recusa a resposta inteira quando os dois aparecem juntos. `origin` em `cors.ts` é **sempre** um array de origens explícitas, nunca a string `'*'` (o default do `hono/cors` quando nenhuma opção é passada) — o resolver de array do `hono/cors` nunca devolve `'*'`, só a origem exata que bateu ou nada, mesmo que `CORS_ALLOWED_ORIGINS` seja configurado por engano como o literal `"*"` (vira o array `['*']`, que nunca bate contra um header `Origin` real de um browser). `src/lib/cors.test.ts` tem a asserção negativa dedicada, inclusive pra esse caso de misconfiguração.

⚠️ **O CORS entra ANTES DE TUDO em `src/index.ts`** — `app.use('*', corsMiddleware<Bindings>())` é a primeira linha depois de `new Hono()`, acima do handler `/api/auth/*` do Better Auth e do catch-all. `app.use('*', ...)` casa com **qualquer** método (inclusive `OPTIONS`): se o catch-all estivesse registrado antes, um preflight `OPTIONS` pra `/api/auth/*` cairia no `404` dele em vez de ser respondido (`204`), e o login quebraria em produção sem mensagem útil nenhuma — só "CORS error" genérico no console do browser, sem indicar a causa real. `cors.test.ts` prova essa ordem contra o app real (`../index`, não só um app de teste isolado): preflight pra `/api/auth/sign-in/social` e pra `/api/auth/get-session` responde `204`, nunca `404`.

⚠️ **Esta fatia NÃO verifica CORS em produção** — a quebra de CORS só aparece lá (local é `localhost` dos dois lados, nunca reproduz o cenário cross-origin real). A verificação em produção é critério de aceitação da fatia ④ (cutover), não desta.

## Correções da revisão final desta fatia (antes do handoff)

Achados da revisão final, aplicados numa leva única (sem segunda rodada) — os fatos ficam aqui porque os relatórios de task/revisão são gitignored. Cada item aponta pro código-fonte em vez de duplicar o raciocínio completo.

- **`resolveSession` agora protege as QUATRO operações de D1 do caminho de todo request autenticado**, não só `getSession`. O `try/catch` era só em torno de `getAuth(c.env).api.getSession(...)`; `buscarGoogleSub` (SELECT em `account`) e `upsertVotacaoUser` (INSERT...RETURNING + SELECT, que lança `Error` explícito em `domain/users.ts`) ficavam de fora — um cenário real (migration `0002` aplicada em produção e a `0001` não, ver pendências abaixo) faria todo `GET /auth/me` estourar `no such table: users` como `500` cru, sem envelope. Estendido em `src/lib/session.ts#resolveSession`; teste dedicado em `session.test.ts` (describe "I1") usa um proxy que deixa passar as duas queries reais de `getSession` (MEDIDO: `session` + `user`, uma consulta cada) e quebra a partir da terceira — verificado por mutação (remover o `catch` novo derruba exatamente esse teste).
- **`trustedOrigins` (`lib/auth.ts`) deriva de `allowedOrigins()` (`lib/cors.ts`), em vez de manter uma lista hardcoded própria.** As duas listas já tinham divergido em produção: o CORS foi apertado pra só `https://piluvitu.com.br`, `trustedOrigins` continuou incluindo `http://localhost:3333`. ⚠️ Isso **não era** "paridade com a Go" — MEDIDO: a Go em produção também não aceita `localhost` (`infra/docker-compose.yml` sobrescreve `CORS_ALLOWED_ORIGINS`); a suposta paridade nunca existiu, eram só as duas listas deste Worker divergindo entre si. Agora `trustedOrigins: [env.BETTER_AUTH_URL, ...allowedOrigins(env.CORS_ALLOWED_ORIGINS)]` — uma fonte de verdade só. Testado em `auth.test.ts` (describe "trustedOrigins deriva de CORS_ALLOWED_ORIGINS (I2)") contra o handler real: com `CORS_ALLOWED_ORIGINS` de produção, `localhost` deixa de ser confiável; sem a var (dev), continua sendo — verificado por mutação.
- **`cors.test.ts` ganhou um teste contra o app REAL numa rota não-preflight que devolve `Response` cru** (`GET /health`, via `okJson`), não `c.json(...)`. Os 19 testes anteriores cobriam só preflight `OPTIONS` (que o `hono/cors` curto-circuita) e um `GET` num app de teste com `c.json(...)` — nenhum provava que o header de CORS sobrevive no caminho `okJson`/`errJson` real (`/health`, `/auth/me`, `/auth/logout`, e as rotas da fatia ②), que depende de um detalhe interno do Hono (`set res()` copiando os headers do `#res` anterior). Funciona hoje (medido) — o teste novo fixa isso; verificado por mutação (removendo `corsMiddleware` de `index.ts`, o teste falha junto dos 3 testes de ordem de montagem já existentes).
- **Corrigida a afirmação de que o `rateLimit` do Better Auth é "a ÚNICA barreira" contra esgotar a cota de escrita do D1** — só documentação, nenhuma mudança de código. MEDIDO: o limiter roda no `onRequest` do router do Better Auth, cobrindo só `/api/auth/*`; `lib/session.ts#resolveSession` chama `auth.api.getSession(...)` **direto** (não via `auth.handler(...)`), então `/auth/me`/`/auth/logout` e as 7 rotas de votação da fatia ② **não passam por throttle nenhum** — mesmo achado já registrado em `apps/financas/CLAUDE.md`. Some-se: todo request autenticado é uma ESCRITA (4 operações no D1 — `getSession`, `buscarGoogleSub`, o upsert incondicional, o SELECT de volta), não só leitura. Comentário corrigido em `lib/auth.ts`. ⚠️ **NÃO tornar o upsert condicional** pra mitigar isso — `ON CONFLICT DO UPDATE ... WHERE <nada mudou>` faz `RETURNING id` não devolver linha e `upsertVotacaoUser` lança (ver `domain/users.ts`); resolver o throttle das rotas guardadas é decisão de fatia futura.
- **`console.warn` de uma linha no fallback de `buscarGoogleSub`** (`lib/session.ts`): se o fallback disparar em produção (sessão sem linha em `account` com `providerId='google'` — não deveria, mas "não deveria" não é "não vai"), grava o id interno do Better Auth em `users.google_sub`, o mesmo defeito que o fix da T4 já corrigiu, e que a fatia ④ duplicaria em usuário + voto órfão. Antes, silencioso; agora, detectável via `wrangler tail`.
- **Índice `account(providerId, accountId)`** — `migrations/0003_account_provider_idx.sql`, espelhando o mesmo índice que o `apps/financas` já criou (leia aquele arquivo pro raciocínio original). ⚠️ **O argumento é mais forte aqui, não o mesmo**: o finanças justificou o índice com "ganho ~zero" (single-user, `account` nunca passa de ~1 linha) — criado só porque índice no D1 é irreversível (só `DROP`+`CREATE`) e o momento mais barato de decidir é antes de tráfego real. Aqui a votação é **livre**: `account` cresce com CADA login social novo, então o ganho de indexar o par `(providerId, accountId)` (consultado a cada sign-in) é real, não hipotético. Aplicado só `--local` nesta leva (nunca `--remote` — ver comando abaixo); `schema.test.ts` atualizado pra 9 índices. ⚠️ Nuance: `wrangler d1 migrations apply ... --remote` aplica TODAS as migrations pendentes de uma vez — quando o dono rodar o comando da pendência "aplicar `0001`/`0002` em produção" (abaixo) depois que este arquivo já existir no repo, a `0003` vai junto na mesma leva, não dá pra aplicar seletivamente as duas primeiras e adiar a terceira via o mesmo comando.

## Segunda leva de correções da revisão final (a que precede o handoff de verdade)

⚠️ **Rótulos reaproveitados, não confundir com a seção acima.** A seção "Correções da revisão final desta fatia (antes do handoff)" já tinha se descrito como "leva única (sem segunda rodada)" — mas uma revisão POSTERIOR (a que gerou esta seção) achou mais itens depois daquele handoff já estar escrito (`git log`: `docs: handoff da migração ramielle + promeia` é anterior a esta leva). Os rótulos `I1`/`I2`/`I3`/`M1`…`M8` abaixo são NUMERADOS DE NOVO a partir de 1 e **não têm relação** com o `I1` (resolveSession), `I2` (trustedOrigins) ou `M4` (índice `account`) já resolvidos na seção acima — duas revisões distintas reusaram a mesma convenção de nomes (`I<n>` = Important, `M<n>` = Minor) pra achados diferentes. Onde a ambiguidade importa, o texto aponta arquivo/linha.

- **I1 — `setSessionWinner` sem guarda deixava a sessão irrecuperável.** A chamada em `POST /close` (`routes/votacao.ts`) fazia `await setSessionWinner(...)` cru: uma falha transitória de escrita no D1 nesse SEGUNDO `UPDATE` propagava pro `onError` global como 500 — DEPOIS que `closeVotingSession` (o PRIMEIRO `UPDATE`) já tinha gravado `status='closed'`. Um retry do admin batia 404 `session_not_open` (o `UPDATE ... WHERE status='open'` já não acha a sessão), fazendo parecer que o close falhou quando `winner_movie_id` já estava gravado — só `winner_method` ficava `NULL` **pra sempre** (nenhuma rota corrige depois; `/tiebreak` recusa com `winner_already_set` porque `winner_movie_id` já não é `null`). Corrigido com `try/catch` (`console.error`, sem propagar) — MESMA paridade do Go (`votes.go:99-103`: `if err != nil { log }`, sem `return`). Testado em `routes/votacao.test.ts` (describe `POST /votacao/sessions/:id/close`, "setSessionWinner falhando..."); verificado por mutação (remover o `catch` derruba exatamente esse teste, 500 em vez de 200).

- **I2 — paridade das MENSAGENS de erro/sucesso com o Go nunca tinha sido medida: 10 de 10 mensagens de erro em `routes/votacao.ts` divergiam byte a byte.** O vetor dourado (`__fixtures__/go-parity.json`) cobre só o SHAPE das structs e a matemática do desempate — nunca as strings dos `errJson`/`okJson`. `apps/web/lib/votacao/api-client.ts:44` usa `primary.message` como `.message` do `ApiError`, e os componentes fazem `toast.error(errorMessage(err))` — depois do cutover (fatia ④), o texto de TODO toast de erro/sucesso da votação muda. Alinhadas ao Go, exatamente (maiúscula inicial + ponto final): as 10 mensagens de `errJson` em `routes/votacao.ts`, mais as 2 de `src/lib/session.ts` (`not_authenticated`/`admin_only` — guards compartilhados por TODA rota protegida, não só votação, então o fix ali conserta tudo de uma vez). O pior caso: `not_authenticated` tinha virado `"requisição sem sessão válida"`, jargão de dev, no lugar de `"Você precisa estar logado."`. **A notificação de SUCESSO também estava errada**: `httpx.Success(msg)` do Go (`internal/httpx/respond.go:88-90`) nunca preenche `Code` (a tag é `json:"code,omitempty"`, que remove a chave inteira quando vazia) — o ramielle tinha inventado `code:'vote_registered'`/`code:'tiebreak_done'`, com as mensagens em minúscula (`"voto registrado"`/`"desempate concluído"`). `Notification.code` (`lib/envelope.ts`) virou opcional (`code?: string`, era `code: string`) pra permitir omitir a chave de vez, igual ao Go. Testado: `envelope.test.ts` (a chave `code` some do JSON cru quando ausente), `session.test.ts` (mensagem exata de `not_authenticated`/`admin_only`, adicionada aos testes já existentes desses guards), `routes/votacao.test.ts` (describe `I2 — mensagens de erro EXATAS` cobrindo `invalid_id`/`session_not_found`/`session_closed`, e as duas notifications de sucesso reforçadas nos testes de happy path de `POST /votes`/`POST /tiebreak`) — todos verificados por mutação.

- **I3 — a organização do domínio (regra antiga: "`sessions.ts` é só leitura, `votes.ts` é escrita") já não fechava, e a fatia ③ bateria nela no dia 1.** `getUserVotedMovieIds` LIA `votes` mas morava em `domain/sessions.ts` (documentado como só-leitura); `closeVotingSession`/`setSessionWinner` ESCREVEM `voting_sessions` mas moravam em `domain/votes.ts`. A fatia ③ traz `POST /votacao/sessions` — escrita em `voting_sessions` + `session_movies` — que sob a regra antiga não teria lugar certo (por leitura/escrita cairia em `votes.ts`, absurdo; pela documentação "só-leitura" de `sessions.ts`, também não caberia). Corrigido: **corte por TABELA/AGREGADO**, não por leitura/escrita. `domain/sessions.ts` agora é dono de `voting_sessions`/`session_movies` (leitura E escrita — `closeVotingSession`/`setSessionWinner` vieram pra cá); `domain/votes.ts` é dono de `votes`/`tiebreaks` (leitura E escrita — `getUserVotedMovieIds` foi pra lá). Cabeçalhos dos dois arquivos reescritos com o raciocínio novo. Movimentação PURA — nenhuma assinatura ou corpo de função mudou, só o arquivo; os testes migraram junto (só o `import` mudou, nenhuma asserção). Verificado por mutação nos dois sentidos: quebrar `getUserVotedMovieIds` (agora em `domain/votes.ts`) derruba os testes do describe homônimo em `votes.test.ts`; quebrar `setSessionWinner` (agora em `domain/sessions.ts`) derruba o de `sessions.test.ts`.

### Correções menores da mesma leva

- **M1** — `GET /sessions/{id}`: `getSessionMovies` ganhou o MESMO `try/catch` que `getUserVotedMovieIds` já tinha — o Go também engole esse erro (`handlers/votacao/sessions.go:182`: `movies, _ := ...`) e devolve `"movies":null` (zero value de slice Go), nunca `500`. Diferente de `voted_movie_ids` (que o Go inicializa `[]int64{}` ANTES do try — por isso vira `[]`, não `null`, quando SUA leitura falha): os dois campos engolem erro, mas com resultados DIFERENTES no JSON, de propósito. Testado com um shim que quebra só `FROM session_movies`; verificado por mutação.
- **M2** — `POST /sessions/:id/votes` agora usa `parseIdDaRota` (a mesma função de `GET /results`/`POST /close`/`POST /tiebreak`/`GET /votes`) em vez de duplicar a checagem de id inline. A duplicação anterior citava "handlers distintos no Go" como justificativa, mas essa distinção só é REAL pra `GET /sessions/{id}` (semântica diferente: aceita `id<=0`) — pra `POST /votes` a semântica é idêntica à das outras rotas de escrita (todas usam `parseID` no Go). Verificado por mutação: remover o `id<=0` de `parseIdDaRota` derruba o teste "id=0" de `POST /votes` também, provando que a rota usa a função de verdade (não uma cópia morta).
- **M3** — um `id` fora da faixa do `int64` (`GET /votacao/sessions/99999999999999999999`, 20 dígitos) ou um `?offset=`/`?limit=` do mesmo tamanho agora respondem 400 `invalid_id`/caem no default, IGUAL ao Go (`strconv.ParseInt`/`Atoi` falham com `ErrRange` nesse caso — a MESMA falha de parse de uma string não-numérica) — antes caíam em 404/usavam um `number` JS impreciso. Novo helper `parseInt64` (`routes/votacao.ts`) usa `BigInt` só pra checar a faixa (nunca pra representar o id — D1/JS já não representam int64 fielmente acima de 2^53, limitação preexistente de todo o domínio). Usado por `atoiOr` (limit/offset) e por `parseIdDaRota`/`GET /sessions/{id}` (id de path). Testado em 3 rotas (`GET /sessions` offset, `GET /sessions/{id}`, `POST /votes`); verificado por mutação nos 3.
- **M5** — o mock de `crypto.getRandomValues` no teste do desempate (`routes/votacao.test.ts`) agora afirma que o spy interceptou EXATAMENTE uma chamada de 32 bytes — antes só checava o RESULTADO final (`winner_movie_id`/`server_nonce` batendo com o vetor dourado), o que deixaria uma chamada EXTRA de 32 bytes passar batida (silenciosa, porque `array.set(nonceEsperado)` é idempotente entre chamadas repetidas com o mesmo array). Verificado por mutação: uma segunda `crypto.getRandomValues(new Uint8Array(32))` inserida no handler (simulando um refactor futuro que gerasse nonce duas vezes) derruba esse teste.
- **M6** — `app.onError` (`src/index.ts`) movido pra ANTES do catch-all. Funcionava onde estava (a ORDEM de registro não importa pro Hono nesse caso específico — `app.onError` não é uma rota, roda pra qualquer exceção não importa quando foi chamado), mas ficava fisicamente abaixo do comentário "SEMPRE POR ÚLTIMO" (que é sobre `app.route()`/rotas), convidando a leitura errada de que `onError` também precisava vir por último. ⚠️ **Nota de precisão corrigida junto**: com o `onError` global, uma exceção que ESCAPE do handler do Better Auth dentro do mount `/api/auth/*` agora sai DENTRO do envelope — o comentário daquele mount (e este arquivo) afirmava sem ressalva "este mount não passa pelo envelope", o que só vale pras respostas NORMAIS dele (sucesso, ou erro que o próprio Better Auth trata), não pra uma exceção não capturada por ele.
- **M7 — os `SELECT` sem `LIMIT` em `domain/votes.ts` (4) e `domain/sessions.ts` (1) são uma EXCEÇÃO deliberada, não descuido — registrada aqui em vez de "corrigida" com `LIMIT`.** `domain/votes.ts`: `SELECT id FROM session_movies` (dentro de `replaceUserVotes`), `listVoteMovieIds`, `listSessionVotesWithUsers`, e `getUserVotedMovieIds` (a 4ª — antes desta leva era "sessions.ts ×1 + votes.ts ×3"; virou "sessions.ts ×1 + votes.ts ×4" como CONSEQUÊNCIA direta da I3 acima, que moveu `getUserVotedMovieIds` pra `votes.ts` trazendo sua própria query sem `LIMIT` junto — não é um item novo, é o mesmo achado original recontado depois do move). `domain/sessions.ts`: `getSessionMovies`. O Go também não limita nenhuma dessas queries, e o fan-out de TODAS é o de UMA sessão só (filmes de uma sessão, votos de uma sessão, linhas do JOIN de uma sessão) — nunca cresce com o total de sessões/usuários do sistema inteiro. **Não acrescentar `LIMIT`** nestas cinco. `listVotingSessions` é a única query cujo fan-out cresce com o tempo (todas as sessões já criadas) e é, por isso, a única que precisa de `LIMIT`/`OFFSET` — já tem, desde a T2.
- **M8 — "provably-fair" foi trocado por "auditável/recomputável"** em `domain/tiebreak.ts`, `domain/votes.ts`, `routes/votacao.ts` e na entrada da T5 acima (§ Estado da fatia ②). "Provably-fair", no sentido técnico do termo, promete não-manipulabilidade PELO SERVIDOR — o que exigiria um commit prévio do `serverNonce` antes de ver a entropia do cliente. Não é o que este desenho faz, nem o que o Go faz. O `server_nonce` sair público na resposta garante RECOMPUTABILIDADE (qualquer pessoa reproduz o sorteio a partir do que a API devolveu e confere o vencedor), não a ausência de manipulação pelo servidor — termo herdado do Go mas afirmado aqui como se fosse um fato MEDIDO, quando nunca foi.
- **M4 — este arquivo afirmava "as 10 rotas de votação da fatia ②"** (na antiga entrada da T6, § _rateLimit_ acima) — são **7** na ② e **9** no total do Go (`/categorias` e `POST /sessions` ficam pra fatia ③, ver § _As duas rotas que ficaram pra fatia ③_). A linha 57 (§ _Fatia ② está COMPLETA_) já estava certa; corrigida só a ocorrência errada. Também estavam CONGELADOS, como se a fatia ① ainda fosse "a atual": o cabeçalho `## Estado desta fatia (① — auth)` (virou `## Estado da fatia ① (auth) — COMPLETA`) e o `- **T5** (esta)` (perdeu o "(esta)" — a fatia ② já tem o seu próprio T6 "(esta)" mais abaixo, marcando o que de fato é o mais recente).
- **Comentário corrigido, comportamento intocado** — o teste de `POST /votes` com corpo `{}` (`routes/votacao.test.ts`) tinha o nome afirmando "paridade com o zero value nil de `[]int64` no Go", mas o Go ECOA `body.MovieIDs` (que é `nil` nesse caso) e serializa `"voted_movie_ids":null`; o ramielle sempre devolveu `[]`. Divergência intencional e inócua (`apps/web` sempre manda `{movie_ids:[...]}` explícito, nunca um corpo `{}` sem a chave) — só o TEXTO do teste foi corrigido, nenhuma asserção mudou.

**Contagem de testes depois desta leva: 247** (`vitest run --reporter=json`, campo `numTotalTests`; 15 arquivos, nenhum arquivo novo). Eram 238 antes desta revisão — **+9**: 1 em `lib/envelope.test.ts` (a chave `code` some do JSON de uma notification de sucesso sem `code`), 8 em `routes/votacao.test.ts` (I1: 1; M1: 1; M3: 3 — offset gigante na listagem, id gigante em `GET /sessions/{id}`, id gigante em `POST /votes`; describe `I2`: 3 mensagens exatas). `lib/session.test.ts`, `domain/sessions.test.ts` e `domain/votes.test.ts` NÃO mudaram de contagem total: o primeiro ganhou 2 asserções novas dentro de testes JÁ EXISTENTES (não conta pro delta); os outros dois só trocaram tests de arquivo entre si por causa da I3 (sessions.test.ts: −3 `getUserVotedMovieIds` +5 `closeVotingSession`/`setSessionWinner` = +2; votes.test.ts: +3 −5 = −2; soma zero).

## Três achados MEDIDOS nas Tasks 2/3, registrados aqui porque os relatórios de task são gitignored

1. **Teste de `STRICT` tem que testar a direção que de fato REJEITA.** Uma coluna `TEXT` numa tabela `STRICT` **CONVERTE** um `INTEGER` recebido em vez de rejeitar (`12345` vira `'12345.0'`) — testar essa direção passa por acidente, sem provar nada. A direção que REALMENTE rejeita (usada em `schema.test.ts`) é `TEXT` não-numérico numa coluna `INTEGER`: mensagem real `cannot store TEXT value in INTEGER column`. Mesmo achado já documentado em `apps/financas/CLAUDE.md` § _Migrations_ — vale igual aqui, mesma versão de SQLite/D1.
2. **SQLite aceita FK apontando pra uma tabela criada DEPOIS.** `voting_sessions.winner_movie_id REFERENCES session_movies(id)` — `session_movies` é criada DEPOIS de `voting_sessions` no `0001_votacao.sql`, e isso não deu erro nenhum: FK é resolvida na primeira ESCRITA que a exercita, não no `CREATE TABLE`. Confirmado aplicando a migration inteira de uma vez (`12 commands executed successfully`, `--local`) sem precisar reordenar as tabelas.
3. **Nenhum `CHECK` do schema portado precisou reposicionar.** A gramática do SQLite (`column-def* table-constraint*` — um `CHECK`/`UNIQUE` de TABELA, não preso a uma coluna, tem que vir depois de todas as column-defs, senão é `syntax error`; já mordeu o finanças, ver `apps/financas/CLAUDE.md` § _Migrations_) nunca chegou a ser exercida aqui: todo `CHECK` do schema original é **column-level** (preso à própria coluna, ex. `status TEXT NOT NULL CHECK (...)`); só os 2 `UNIQUE` são de tabela, e já estavam na última posição antes do `0001_votacao.sql` ser escrito.

## Comandos

```bash
make dev-ramielle           # wrangler dev na porta 8788 (8787 já é do finanças)
make test-ramielle          # pnpm --filter @piluvitu/ramielle test

pnpm --filter @piluvitu/ramielle run lint     # tsc --noEmit
pnpm --filter @piluvitu/ramielle run test     # vitest run

# Migrations
pnpm --filter @piluvitu/ramielle exec wrangler d1 migrations apply piluvitu-ramielle --local
pnpm --filter @piluvitu/ramielle exec wrangler d1 migrations apply piluvitu-ramielle --remote
pnpm --filter @piluvitu/ramielle exec wrangler d1 migrations list piluvitu-ramielle --local
pnpm --filter @piluvitu/ramielle exec wrangler d1 migrations list piluvitu-ramielle --remote
```

**Contagem de testes ao fim da T6: 238** (histórico — NÃO é o número atual, ver "Contagem de testes depois desta leva: 247" no início deste arquivo, § _Segunda leva de correções da revisão final_). Recontar sempre via `pnpm --filter @piluvitu/ramielle exec vitest run --reporter=json` (campo `numTotalTests`), nunca só o resumo condensado do terminal — este número já mudou 3× nesta fatia (191→216 na T5, 216→238 na T6, 238→247 na revisão final). Composição na T6 (fatia ②, ÚLTIMA task de rota nova): os 216 até a T5, mais 22 novos daquela task — 6 em `domain/votes.test.ts` (describe `listSessionVotesWithUsers`: vazio, sessão inexistente, happy path com JOIN completo, voto de aprovação não-colapsado, isolamento entre sessões, ordenação por `created_at ASC`), 8 em `routes/votacao.test.ts` (describe `GET /votacao/sessions/:id/votes`: 401 → 403 não-admin ANTES do happy path, por brief → 3× `invalid_id` → sessão inexistente 200 zeros → happy path snake_case/`toIsoUtc` → voto de aprovação 2 linhas), 8 em `index.test.ts` (1 no describe `app.onError global` + 7 no `test.each` de montagem das 7 rotas de votação acima do catch-all).

## Pendências do dono — nada disto foi feito nesta fatia

- **Aplicar as migrations `0001`/`0002`/`0003` em produção** (`--remote`, comando acima) — hoje só rodaram `--local`. ⚠️ A `0003` (índice `account(providerId, accountId)`, § _Correções da revisão final desta fatia (antes do handoff)_ acima — não confundir com o `M4` da § _Segunda leva_, que é sobre contagem de rotas) foi criada DEPOIS de `0001`/`0002` já estarem só `--local` — o comando `--remote` aplica as três de uma vez, não seletivamente.
- **Cadastrar os secrets** (`wrangler secret put <NOME>`, do diretório `apps/ramielle`): `BETTER_AUTH_SECRET` (gerar com `openssl rand -base64 32`, nunca reusar o valor de `.dev.vars`), `ADMIN_EMAILS` (hoje está em `wrangler.jsonc#vars` com o e-mail do dono — mover pra secret ou atualizar o var se a lista crescer antes do primeiro deploy real), e **`GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET`**. ⚠️ **Estes dois NÃO são de um OAuth Client novo — são os valores do MESMO OAuth Client que hoje já serve a área de admin de outro app e a API Go** (ver bullet da redirect URI abaixo). Criar um client novo pro ramielle quebraria a premissa do bullet seguinte: um client novo não tem URI nenhuma pra preservar, e o aviso "adicionar, nunca substituir" perderia o sentido.
- **Cadastrar os secrets do Sheets** (fatia ③, Task 2, `wrangler secret put <NOME>`): `GOOGLE_SA_JSON` (o JSON INTEIRO da service account que já lê a planilha de filmes na Go — reusar a mesma, não criar uma nova), `GSHEETS_MOVIES_SPREADSHEET_ID` (ID da planilha, extraído da URL do Sheets — mesmo valor que `apps/api/.env` já usa), e opcionalmente `GSHEETS_MOVIES_RANGE` (default `'A2:F'` já embutido no código se ausente). Sem os dois primeiros, `GET /votacao/categorias` e `POST /votacao/sessions` respondem `503 sheets_disabled` — comportamento seguro, não quebra nada em produção (que ainda fala com a Go).
- **Cadastrar o secret do TMDb** (fatia ③, Task 4, `wrangler secret put TMDB_API_KEY`): mesma chave que `apps/api/.env` já usa (`TMDB_API_KEY`, https://themoviedb.org/settings/api). Sem ela, `POST /votacao/sessions` cria a sessão normalmente, só SEM pôsteres — nunca erro (mesma paridade do Go).
- **Criar o Custom Domain** `ramielle.piluvitu.com.br` (dashboard: Workers & Pages → `piluvitu-ramielle` → Settings → Domains & Routes → Add → Custom Domain) — `wrangler.jsonc` já declara a rota (`routes: [{ pattern: "ramielle.piluvitu.com.br", custom_domain: true }]`), falta o domínio existir de fato.
- **Registrar a redirect URI no Google Console, no MESMO OAuth Client do bullet acima**: `https://ramielle.piluvitu.com.br/api/auth/callback/google`. ⚠️ **ADICIONAR, NUNCA SUBSTITUIR AS EXISTENTES** — é justamente por ser o mesmo client (não um novo) que esta URI é ADITIVA: ele hoje serve a área de admin de outro app **e** a API Go (`promeia.piluvitu.com.br/auth/google/callback`); remover uma URI existente quebra o que já está no ar.

## Nada em produção mudou nesta fatia

`apps/web` continua falando com a Go (`promeia.piluvitu.com.br`, ver `apps/api/CLAUDE.md`). Nenhuma tela, nenhum endpoint em produção mudou — tudo o que este arquivo descreve (auth, CORS, guards) só é alcançável localmente/via preview até o dono completar as pendências acima.
